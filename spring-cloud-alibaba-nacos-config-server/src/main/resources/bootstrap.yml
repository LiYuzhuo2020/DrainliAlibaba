server:
  port: 8083
spring:
  jmx:
    enabled: true
  application:
    name: spring-cloud-alibaba-config-server
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: localhost:8848
        prefix: userInfo
      discovery:
        enabled: true
        server-addr: localhost:8848
#user:
#  - name: drainli
#  - age: 24
#  - height: 170cm